<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.airtnt.airtnt.service.HostMapper">
	
	<select id="listGuide" resultType="GuideDTO">
		select 
			id, subject, 
			reg_date as regDate, 
			mod_date as modDate, 
			explanation, content1, content2, content3 from guide
	</select>
	<select id="getGuide" parameterType="int" resultType="guideDTO">
		select 
			id, subject, 
			reg_date as regDate, 
			mod_date as modDate, 
			explanation, content1, content2, content3 
		from guide where id = #{id}
	</select>
	<insert id="insertProperty" parameterType="propertyDTO">
		insert into property values(property_seq.nextval, #{name}, #{hostId}, 
		#{propertyDesc}, #{address}, #{latitude}, #{longitude}, #{price}, #{bedCount}, 
		#{maxGuest})<!-- modeDate==null -->
	</insert>
	<delete id="deleteProperty" parameterType="int">
		
	</delete>
	<select id="propertyType" resultType="propertyTypeDTO">
		select 
			id, name, 
			is_use as isUse, 
			reg_date as regDate, 
			mod_date as modDate 
		from property_type order by id
	</select>
	<select id="subPropertyType" parameterType="int" resultType="subPropertyTypeDTO">

		select 
			id, name, 
			property_type_id as propertyTypeId,
			is_use as isUse, 
			reg_date as regDate, 
			mod_date as modDate from sub_property_type where property_type_id = #{propertyTypeId}

	</select>
	<select id="roomType" resultType="roomTypeDTO">
		select 
			id, name, 
			is_use as isUse, 
			reg_date as regDate, 
			mod_date as modDate 
		from room_type
	</select>
	<select id="listBooking" parameterType="String" resultType="BookingDTO">
		select 
			booking.id, 
			booking.property_id as propertyId, 
			booking.guest_id as guestId, 
			booking.host_id as hostId, 
			booking.guest_count as guestCount, 
			booking.day_count as dayCount, 
			booking.total_price as totalPrice, 
			booking.booking_number as bookingNumber, 
			booking.check_in_date as checkInDate, 
			booking.check_out_date as checkOutDate, 
			booking.confirm_date as confirmDate, 
			booking.reg_date as regDate, 
			booking.mod_date as modDate, 
			member.name as guestName,
            member.id as memberId 
		from booking, member
        where booking.guest_id = member.id
        and booking.host_id = #{hostId}
        </select>
	<select id="getMemberDTO" parameterType="String" resultType="MemberDTO">
		select * from member where id = #{memberId};
	</select>
	<select id="listProperty" parameterType="String" resultType="PropertyDTO">
		select 
			p.id, p.name,
			p.host_id as hostId,
			p.address,
			p.price,
			p.bed_count as bedCount,
			p.max_guest as maxGuest,
			p.reg_date as regDate,
			p.mod_date as modDate,
			p.room_type_id as roomTypeId,
			r.id,
			r.name as roomTypeName,
            p.sub_property_type_id as subPropertyTypeId,
            s.id,
            s.name as subPropertyTypeName
		from property p, sub_property_type s, room_type r
		where host_id = #{hostId}
		and r.id = p.room_type_id
		and p.sub_property_type_id =  s.id
	</select>
	<select id="listTransaction" parameterType="String" resultType="TransactionDTO">
		select 
			b.id as booking_table_id, 
			b.host_id as hostId, 
			b.total_price as totalPrice,
			b.confirm_date as confirmDate,
			b.property_id as propertyId,
			b.booking_number as bookingNumber,
			b.check_out_date as checkOutDate,
			t.id,
			t.booking_id as bookingId,
			t.is_refund as isReFund,
			t.site_fee as siteFee,
			t.pay_expt_date as payExptDate,
			t.reg_date as regDate,
			t.mod_date as modDate
		from transaction t, booking b
        where b.id = t.booking_id
        and b.host_id = #{hostId}
	</select>
	<select id="totalEarningMap" parameterType="String" resultType="TransactionDTO"> 
		select 
			b.total_price as totalPrice,
			b.id,
			b.host_id as hostId,
			t.site_fee as siteFee,
			t.booking_id,
			t.is_refund as isRefund,
			t.pay_expt_date as payExptDate
		from booking b, transaction t
		where b.id = t.id
        and sysdate > t.pay_expt_date
		and host_id = #{hostId}
	</select>
	<!-- <insert id="saveImage" parameterType="java.util.Map">
    I	insert into img_table VALUES #{img}    
	</insert> -->
	<!--<delete>
	
	</delete>
	<update>
		
	</update> -->
</mapper>
